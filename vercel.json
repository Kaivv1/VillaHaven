{
  "version": 2,
  "builds": [
    { "src": "./server/index.js", "use": "@vercel/node" },
    { "src": "./src/**/*", "use": "@vercel/vite" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/src/$1" },
    { "src": "/server/(.*)", "dest": "/server/$1" }
  ]
}
